spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PW}
  data:
    redis:
      host: ${REDIS_HOST}
      port: 6379
  mail:
    username: ${MAIL_USERNAME_1}
    password: ${MAIL_PASSWORD_1}